import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.facultadtelematica.incidentes',
  appName: 'Registro de Incidentes',
  webDir: 'dist'
};

export default config;
